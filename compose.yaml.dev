services:
  diyws:
    image: diyws-dev
    build: .
    ports:
      - "3000:3000"
    volumes:
      - /app/node_modules
      - .:/app
      - /var/run/docker.sock:/var/run/docker.sock
    develop:
      watch:
        - action: sync
          path: .
          target: /app
